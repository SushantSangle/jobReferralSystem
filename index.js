/**
 * @format
 */

import {AppRegistry} from 'react-native';
import LoginScreen from './routes/LoginStack';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => LoginScreen);
